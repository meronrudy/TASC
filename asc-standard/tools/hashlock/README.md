# hashlock

Creates a deterministic SHA-256 manifest for evidence files.

This includes TASC assurance packs and profile conformance manifests when present in `evidence/manifests/`.

## Usage

```bash
python3 tools/hashlock/hashlock.py --repo-root .
```
